#pragma once

#include <bits/stdc++.h>
#include <string>
#include <vector>
#include <forward_list>

// МТФ применять только к текстовым данным!!

void MTF_compress(std::vector<char> &input, std::vector<char> &output);
void MTF_decompress(std::vector<char> &input, std::vector<char> &output);
